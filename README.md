# Simple-Automatic-Clock
This project is a simple JavaScript web application that displays the current date and time in real time.

The clock starts when the user clicks a button and updates every second.

## 🚀 Features

- Start the clock with a button click
- Displays current date and time
- Updates every second in real time
- Prevents multiple intervals from running
- Clean, centered layout
- Styled with CSS and Google Font (Poppins)

## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (Vanilla JS)

## 📂 Project Structure

.
├── index.html
├── styles.css
├── script.js
└── README.md


## ▶️ How to Run the Project

1. Clone the repository:
   ```bash
   git clone <your-repository-link>
Open the project folder

Open index.html in a web browser

Click "Click This Button To Start The Clock"

📌 How It Works
A button click starts a setInterval

The Date object gets the current time

The display updates every 1 second

Additional clicks are ignored to avoid duplicate timers

🧑‍💻 Author
Created by Dairon Revival
